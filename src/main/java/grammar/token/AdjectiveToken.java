package grammar.token;

public class AdjectiveToken extends Token {
    public static final String CORRESPONDING_NLG_CATEGORY = "ADJECTIVE";
    public AdjectiveToken(String originalString) {
        super(originalString);
    }
}
