package grammar.token;

public class NounToken extends Token {
    public static final String CORRESPONDING_NLG_CATEGORY = "NOUN";
    public NounToken(String originalString) {
        super(originalString);
    }
}
