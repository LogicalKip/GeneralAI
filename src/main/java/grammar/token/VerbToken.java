package grammar.token;

public class VerbToken extends Token {
    public static final String CORRESPONDING_NLG_CATEGORY = "VERB";
    VerbToken(String originalString) {
        super(originalString);
    }
}
