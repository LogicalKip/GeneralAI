package grammar;

/**
 * The word(s) in a question whose concepts are unknown and asked for.
 * E.g Who, what thing (what entity), what action, possibly even why (what reason) or how much (what quantity)
 *
 */
public interface InterrogativeWord {
}
