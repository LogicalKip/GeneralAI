package grammar;

/**
 * Shows that, in a sentence, the class implementing may represent the part where an entity is expected in the final structure
 * Ex : "who" would be a suitable word in any place an entity is expected (as in "ENTITY is here").
 * "the man" would also be.
 * @author charles
 *
 */
public interface IEntity {
}
